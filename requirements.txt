# Core Django (minimal)
Django==5.0.3
psycopg2-binary==2.9.9  # or your database adapter
whitenoise==6.6.0
gunicorn==21.2.0

# Minimal ML (remove if not absolutely needed)
scikit-learn==1.4.1.post1 --no-deps  # without dependencies
numpy==1.26.4
pandas==2.2.1 --no-deps
